HOTFIX for Phase C no-reply

1. Replace api/telegram.js on GitHub
2. Vercel Ready
3. Open once: https://radiant-queen-pasiya-max-v2.vercel.app/api/telegram?setup=1
4. Test /start then /logrun test then /streak

Changes:
- Removed ctx.react (breaks some Telegraf builds)
- Uses setMessageReaction via callApi (ignored if fails)
- addRunXp resilient if streak columns missing
